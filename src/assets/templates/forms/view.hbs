import React from 'react';
import {View} from 'react-native';
import SafeAreaView from 'react-native-safe-area-view';
import {appStyle, formStyle} from '../assets/styles';
import {KeyboardAwareScrollView} from 'react-native-keyboard-aware-scroll-view';
import {{schemaFormName}} from '../forms/{{schemaFormName}}';

const {{schemaName}} = ({navigation}) => {
return (
<SafeAreaView style={appStyle.panSafeArea}>
  <KeyboardAwareScrollView
    contentContainerStyle={appStyle.panKeyboard}
    keyboardShouldPersistTaps="handled">
    <View style={appStyle.panMain}>
      <View style={[formStyle.container, appStyle.panLogin]}>
        <{{schemaFormName}} navigation={navigation} />
      </View>
    </View>
  </KeyboardAwareScrollView>
</SafeAreaView>
);
};

export default {{schemaName}};
